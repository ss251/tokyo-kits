# Public testnet gRPC, not the retired Sui JSON-RPC API.
SUI_GRPC_URL=https://fullnode.testnet.sui.io:443
# Demos create and retain three testnet keys only in ignored .run/accounts.json.
# bun scripts/demo.ts accounts prints public addresses, never the keys.
# Obtain official testnet USDC for the payer through https://faucet.circle.com.
# Never add production keys or self-minted substitutes for official USDC.
