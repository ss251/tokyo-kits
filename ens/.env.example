# Optional archive endpoint; no credential is needed for the default.
ENS_RPC_URL=https://ethereum-sepolia.publicnode.com
# Optional reproducible fork block
# FORK_BLOCK_NUMBER=
# Example name from a current demo; fork-only names need the retained fork RPC.
# ENS_DEMO_NAME=
