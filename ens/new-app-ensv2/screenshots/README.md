# Local fork browser evidence

Captured2026-09-14 JST from the built Next.js app reading the same retained
Sepolia fork as the saved component receipts. Both names resolved to the
recorded recipient and limit7. The browser refused8 units and accepted5
as an explicit local preview. Desktop and390px mobile checks passed with
no horizontal overflow. These screenshots are not a public hosted demo.

The final API repeats the block-hash read after record resolution and rejects
a changed snapshot. It returned matching configuration for both names,400
for invalid/zero inputs, and502 for an unregistered name without fallback.
See [ui-evidence.json](ui-evidence.json).
